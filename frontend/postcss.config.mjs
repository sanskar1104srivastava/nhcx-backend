/**
 * Tailwind CSS v4 (via @tailwindcss/vite) wires up its own PostCSS plugins —
 * `tailwindcss` and `autoprefixer` must NOT be listed here.
 * This file exists only as a hook for any extra PostCSS plugins.
 */
export default {};
